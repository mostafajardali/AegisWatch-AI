# CyberAI Watch Starter

Projet educatif: mini-SIEM avec detection par regles et detection d anomalies.

## Lancer

```bash
pip install -r requirements.txt
python src/rules_engine.py
streamlit run dashboard/app.py
```

Placez `cyberai_watch_logs_start.csv` dans le dossier `data/`.
